import styled from "@mui/material/styles/styled";
 import {Box} from '@mui/material' 

const AboutSectionContainer = styled(Box)(({ theme }) => ({ 
 display: "flex", 
}));

export { AboutSectionContainer };