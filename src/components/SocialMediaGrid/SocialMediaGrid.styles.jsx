import styled from "@mui/material/styles/styled";
 import {Box} from '@mui/material' 

const SocialMediaGridContainer = styled(Box)(({ theme }) => ({ 
 display: "flex", 
}));

export { SocialMediaGridContainer };